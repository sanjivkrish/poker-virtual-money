'use strict';

describe('Service: arenaInfo', function () {

  // load the service's module
  beforeEach(module('pokerVirtualMoneyApp'));

  // instantiate service
  var arenaInfo;
  beforeEach(inject(function (_arenaInfo_) {
    arenaInfo = _arenaInfo_;
  }));

  it('should do something', function () {
  });

});
